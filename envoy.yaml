node:
  cluster: waf_cluster
  id: waf_node

admin:
  access_log_path: "/dev/null"
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 9901

dynamic_resources:
  cds_config:
    path: "/etc/envoy/dynamic/cds.yaml"
  lds_config:
    path: "/etc/envoy/dynamic/lds.yaml"

static_resources:
  listeners: []
  clusters: []
